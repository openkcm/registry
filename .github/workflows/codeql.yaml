name: "CodeQL::Analyze"

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"
  schedule:
    - cron: '16 11 * * 5'

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  analyze:
    uses: openkcm/build/.github/workflows/codeql.yaml@7410b3341d4752b66e1b68327f8a9d8ae3ed81c0 # main
