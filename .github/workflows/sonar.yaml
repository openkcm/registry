name: Sonar Analysis Secure

on: # zizmor: ignore[dangerous-triggers]
  workflow_run:
    workflows: ["Quality"]
    types:
      - completed

permissions:
  contents: read
  statuses: write

jobs:
  check:
    uses: openkcm/build/.github/workflows/sonar.yaml@a3b997812361a5824f246b6fc600284aafb05d5a # main
    secrets: inherit # zizmor: ignore[secrets-inherit]
