name: New Release (Manual Trigger)

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  release:
    uses: openkcm/build/.github/workflows/release.manual.yaml@a3b997812361a5824f246b6fc600284aafb05d5a # main
    secrets: inherit # zizmor: ignore[secrets-inherit]

