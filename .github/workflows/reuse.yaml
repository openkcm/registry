name: Compliance

on:
  push:
    paths-ignore:
      - 'charts/**'
      - 'docs/**'
      - 'LICENSES/**'
      - '.releases/**'
      - '.github/**'
  pull_request:

permissions:
  contents: read

jobs:
  check:
    uses: openkcm/build/.github/workflows/reuse.svc.yaml@7410b3341d4752b66e1b68327f8a9d8ae3ed81c0 # main
    secrets: inherit # zizmor: ignore[secrets-inherit]
