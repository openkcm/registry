name: Helm

on:
  push:
    branches: [ main ]
    paths:
      - 'charts/**'

permissions:
  packages: write

env:
  OCI_URL: ghcr.io/openkcm

jobs:
  release:
    uses: openkcm/build/.github/workflows/chart-release.svc.yaml@a3b997812361a5824f246b6fc600284aafb05d5a # main
    secrets: inherit # zizmor: ignore[secrets-inherit]
