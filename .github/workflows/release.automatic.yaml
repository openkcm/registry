name: Release New Version

on:
  pull_request:
    types:
      - closed

permissions:
  contents: write

jobs:
  release:
    uses: openkcm/build/.github/workflows/release.svc.yaml@a3b997812361a5824f246b6fc600284aafb05d5a # main
    secrets: inherit # zizmor: ignore[secrets-inherit]
